# Build artifacts
build/
dist/
**/hevolveai_manifest.json
# release_manifest.json at project root is now tracked (see !release_manifest.json below)
# Only ignore nested copies (e.g. in build/ or dist/ which are already ignored)
build/**/release_manifest.json
Output/
hartos_backend_src/
landing-page/build/     # React build output - regenerated by npm run build
static/landing-page/    # Old build artifacts - should not be tracked
*.exe
!Nunba.exe

# Embedded Python pip packages (1.4 GB - installed by build.py at build time)
python-embed/Lib/site-packages/
python-embed/Scripts/

# WebView2 bootstrapper (downloaded during build)
MicrosoftEdgeWebview2Setup.exe

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Local config (may contain secrets)
.env
.env.local
.env.production
# BUT track .env.development (build flags only, no secrets)
!landing-page/.env.development
*.local.json
secrets.json

# Release manifest & app config are gitignored by default BUT the repo
# ships default stubs so the installed exe passes boot verification.
# Force-track the project-root copies (no secrets, no machine paths).
!config.json
!release_manifest.json

# Node modules (should be installed fresh)
node_modules/
landing-page/node_modules/

# Package locks (can cause issues across platforms)
# package-lock.json  # Uncomment if causing issues

# Temporary files
*.tmp
*.temp
nul
*.stackdump

# Test/debug scripts (root-level only)
/test_launch*.ps1
/test_source_launch.bat
/test_splash.py
/run_voice_tests.ps1
/debug_hang.py
/debug_startup.py
/test_server_debug.py

# Log/output artifacts
/source_launch_stderr.txt
/source_launch_stdout.txt
/flask_app.log
/staged_js_files.txt

# Analysis/plan docs (personal)
/gapanalysisforsathish.*
/planforsathish.md

# Bootstrap scripts (large, downloadable)
/get-pip.py

# pytest
.pytest_cache/

# Backup venvs
.venv.bak/

# Database files (contain user data)
*.db
*.sqlite
*.sqlite3

# TrueFlow runtime injector (deployed by plugin, not source)
.pycharm_plugin/

# Claude Code
.claude/

# Credentials and keys
*.pem
*.key
credentials.json
service-account.json
/static/landing-page/static/
config.json
*.pem
master_private_key*
